{% if languages|length > 1 %}
<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      {% for language in languages %}
          {% if language.code == code %}
            <span class="flag">
              <img src="catalog/language/{{ language.code }}/{{ language.code }}.png" alt="{{language.name }}" title="{{ language.name }}">
            </span>{{ text_language  }}<span class="caret"></span>
          {% endif %}
      {% endfor %}
  </a>
  <ul class="dropdown-menu">

    <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-language">
        {% for language in languages %}
          <li>
            <a href="{{ language.code }}" name="{{ language.code }}">

              <img src="catalog/language/{{ language.code }}/{{ language.code }}.png" alt="{{language.name }}" title="{{ language.name }}">
                {{ language.name }}</a>
          </li>
        {% endfor %}

      <input type="hidden" name="code" value="" />
      <input type="hidden" name="redirect" value="{{ redirect }}" />
    </form>

    <li role="separator" class="divider">
    <li><a href="#">Separated link</a></li>
    <li role="separator" class="divider">
    <li><a href="#">One more separated link</a></li>
  </ul>
</li>
{% endif %}
{#<li class="dropdown">#}
  {#<a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="flag">
  <img src="catalog/view/theme/styletour/img/flag.jpg" alt=""></span>

  English <span class="caret"></span></a>#}
  {#<ul class="dropdown-menu">#}
    {#<li><a href="#">France</a></li>#}
    {#<li><a href="#">Russia</a></li>#}
    {#<li><a href="#">Something else here</a></li>#}
    {#<li role="separator" class="divider"></li>#}
    {#<li><a href="#">Separated link</a></li>#}
    {#<li role="separator" class="divider"></li>#}
    {#<li><a href="#">One more separated link</a></li>#}
  {#</ul>#}
{#</li>#}